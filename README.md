# PROYECTO ALGEBRA

Algebra/
├── src/main/java/com/calculadora/
│   ├── modelo/       # Entidades base
│   │   └── Matriz.java      <- (Alex) Clase con la estructura y validaciones.
│   ├── logica/       # Algoritmos de cálculo
│   │   └── Calculadora.java <- (Alex) Métodos static: sumar, multiplicar, etc.
│   └── ui/           # Interfaz de usuario
│       └── Menu.java        <- (Compañero) Lógica de la CLI y Scanner.
├── pom.xml           # Configuración para el LSP (jdtls) y Maven.
└── .gitignore        # Archivos excluidos (binarios, caché de IDE).

